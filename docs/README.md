# Pilgrim Tracker Documentation Bundle

Place these files in the project's `docs/` directory.

Start with `DOCS_MANIFEST.md`.

Current verified baseline:

- `flutter analyze`: no issues
- `flutter test`: 93 tests passing
- native SQLite schema version: 5
- current milestone: asset portfolio and online-price foundation
